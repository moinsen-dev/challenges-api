node_modules/
.wrangler/
coverage/
dist/
.DS_Store
*.log
.dev.vars

# Session handoff between Claude sessions. Internal working state by the same
# rule as notes/ below — it names open decisions and moods, not the software.
STATE.md

# Internal working notes in German: strategy, pricing thinking, session state.
# Remove this line if you decide to publish them — the docs/ folder is the
# public, English counterpart.
notes/

# Build artefacts of the client packages. Regenerated by `dart pub get` and by
# Godot's first import; a snapshot compiled against one SDK is noise in a diff
# and wrong on anyone else's machine.
.dart_tool/
.godot/

# Generated boundary import, see scripts/geo-import.mjs
.geo/
